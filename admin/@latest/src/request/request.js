const handlers=new Map();const key=(m,u)=>`${m.toUpperCase()} ${u}`;export const registerMockRoute=(m,u,h)=>handlers.set(key(m,u),h);async function send(m,u,p){const h=handlers.get(key(m,u));if(!h)throw new Error(`未找到静态接口：${m} ${u}`);await new Promise(r=>setTimeout(r,60));return{code:200,message:'success',data:await h(p)}}export default{get:(u,c={})=>send('GET',u,c),post:(u,b={})=>send('POST',u,b),put:(u,b={})=>send('PUT',u,b),delete:(u,c={})=>send('DELETE',u,c)}
